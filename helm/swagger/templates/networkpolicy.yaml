apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: swagger-deny-all
spec:
  podSelector:
    matchLabels:
      app: {{ include "swagger.name" . }}
  policyTypes:
    - Ingress
    - Egress
